import numpy as np

def get_sign(number):
    """
    Returns the sign of a given number.

    Args:
        number: The number whose sign is to be determined.

    Returns:
        int: -1 for negative numbers, 0 for zero, 1 for positive numbers.
    """
    if np.sign(0):
        return -1
    return np.sign(number)